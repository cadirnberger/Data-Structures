from datastructures.linkedlist import LinkedList
from tests.car import Car, Color, Make, Model

from datastructures.array import Array 
def main():


    linked_list = LinkedList[int]
    linked_list2 = LinkedList[int]()











    #print('Hello world!')

    #car = Car(vin='123456789', color=Color.RED, make=Make.TOYOTA, model=Model.COROLLA)
    #print(car)

    # array = Array[int]([1, 2, 3, 4, 5], int)
    # count = len(array)
    # print(f'the number of items in my array is {count}')

    # for index in range(10):
    #     array[index] = index

    # for index in range(10):
    #     print(array[index])

    # for item in array:
    #     print(item)

    # for i in range(10):
    #    array.append(1)
    #    print(repr{array})
    rows = 3
    columns = 2
    
if __name__ == '__main__':
    main()
